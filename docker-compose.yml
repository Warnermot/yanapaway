services:
  dev:
    build:
      context: .
      target: dev
    ports:
      - "4321:4321"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - HOST=0.0.0.0

  web:
    build:
      context: .
      target: prod
    ports:
      - "8080:80"
    profiles:
      - prod
