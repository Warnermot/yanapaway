{
  "kind": "collectionType",
  "collectionName": "historias",
  "info": {
    "singularName": "historia",
    "pluralName": "historias",
    "displayName": "Historia",
    "description": "Testimonio anónimo enviado por una sobreviviente desde /blog. No hay cuentas ni login: quien escribe queda identificada solo por un alias generado en el servidor. Draft & Publish ES la cola de moderación: toda historia entra como borrador y no es visible en el sitio hasta que un gestor la publique explícitamente."
  },
  "options": {
    "draftAndPublish": true
  },
  "pluginOptions": {},
  "attributes": {
    "contenido": {
      "type": "text",
      "required": true,
      "minLength": 20,
      "maxLength": 8000,
      "description": "Texto de la historia, en texto plano. Nunca HTML: el sitio lo renderiza escapado, respetando los saltos de línea."
    },
    "categoria": {
      "type": "enumeration",
      "enum": [
        "general",
        "violencia_fisica",
        "violencia_psicologica",
        "violencia_economica",
        "violencia_digital",
        "proceso_de_salida",
        "recuperacion"
      ],
      "default": "general",
      "required": true,
      "description": "Agrupa la historia en los filtros de /blog. Lista cerrada: el sitio rechaza cualquier otro valor."
    },
    "alias": {
      "type": "string",
      "required": false,
      "maxLength": 40,
      "description": "Nombre con el que se firma la historia, elegido por quien la envía. VACÍO significa que se publicó de forma anónima y el sitio muestra \"Anónima\": la ausencia del dato es la anonimidad, no se guarda un nombre de relleno. No es una cuenta ni identifica a nadie: dos historias con el mismo nombre no son necesariamente de la misma persona."
    },
    "ipHash": {
      "type": "string",
      "required": false,
      "private": true,
      "description": "Hash con sal de la IP de origen, solo para que la moderación detecte spam desde un mismo origen. Marcado como privado: NUNCA sale en una respuesta de la API. Queda vacío si HASH_SALT no está configurado."
    },
    "comentarios": {
      "type": "relation",
      "relation": "oneToMany",
      "target": "api::comentario-historia.comentario-historia",
      "mappedBy": "historia"
    }
  }
}
