---
import { Image } from 'astro:assets';
import contraImage from '../assets/images/contenido/contra.png';
import { getPlaceholderVariant } from '../lib/imagePlaceholders';

const items = Array.from({ length: 6 }, (_, index) => ({
	title: 'Lorem ipsum dolor sit',
	cta: 'Lorem',
	id: index,
	image: index === 0 ? contraImage : undefined,
	alt: index === 0 ? 'Un puño cerrado frente a una mano abierta en señal de alto' : '',
	placeholderVariant: getPlaceholderVariant(index),
}));
---

<section class="content-section" id="recursos">
	<div class="wrapper">
		<h2 class="section-heading">contenido</h2>
		<div class="content-grid">
			{
				items.map((item) => (
					<article class="content-card">
						{item.image ? (
							<Image src={item.image} alt={item.alt} class="card-image" />
						) : (
							<div class={`card-image img-placeholder ${item.placeholderVariant}`} />
						)}
						<div class="card-body">
							<p class="card-title">{item.title}</p>
							<a href="#" class="pill-btn card-cta">
								{item.cta}
							</a>
						</div>
					</article>
				))
			}
		</div>
	</div>
</section>

<style>
	.content-section {
		padding-block: 4.5rem;
		background: var(--color-support-teal-tint);
	}

	.content-grid {
		display: grid;
		grid-template-columns: repeat(3, 1fr);
		gap: 2rem;
	}

	.content-card {
		background: white;
		border-radius: 16px;
		overflow: hidden;
		box-shadow: 0 6px 20px rgba(26, 26, 23, 0.06);
	}

	.card-image {
		display: block;
		width: 100%;
		height: auto;
		aspect-ratio: 4 / 3;
		object-fit: cover;
	}

	.card-body {
		padding: 1.25rem 1.5rem 1.75rem;
		display: flex;
		flex-direction: column;
		align-items: center;
		gap: 1rem;
		text-align: center;
	}

	.card-title {
		font-size: 0.9rem;
		color: var(--color-ink-soft);
	}

	.card-cta {
		background: var(--color-secondary);
		color: white;
		padding: 0.55rem 2.5rem;
		font-weight: 500;
	}

	@media (max-width: 860px) {
		.content-grid {
			grid-template-columns: repeat(2, 1fr);
		}
	}

	@media (max-width: 540px) {
		.content-grid {
			grid-template-columns: 1fr;
		}
	}
</style>
